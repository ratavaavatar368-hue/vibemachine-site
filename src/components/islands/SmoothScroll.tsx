// SmoothScroll — deprecated.
// Lenis intercepts native scroll and applies it via JS main thread → micro-stutters.
// Replaced by: CSS scroll-behavior smooth (anchor links) + native compositor scroll.
export default function SmoothScroll() { return null; }
